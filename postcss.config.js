// /** @type {import('postcss-load-config').Config} */ // Optional: Remove this line if it causes issues, though it's usually fine.
// Remove the variable assignment and export directly
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

